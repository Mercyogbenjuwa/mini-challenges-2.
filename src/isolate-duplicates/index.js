function isolateDuplicates(text) {
	
		let s = new Set();
		
		
		for (let i = 0;i<n;i++)
			s.add(str[i]);

		
		for (const v of s) {

			document.write(v);
	}
	}

	
		let text = "aisha";
		text = str.length;

		isolateDuplicates(str, n);

}

module.exports = isolateDuplicates;
